# Go
/backend/bin/
*.test
*.out
coverage.*

# Frontend
/frontend/node_modules/

# Frontend build output: only backend/internal/web/dist/index.html's
# checked-in placeholder is tracked. Real builds (`make frontend-build`)
# land here too but aren't committed -- CI/Docker regenerate them.
/backend/internal/web/dist/assets/
/backend/internal/web/dist/favicon.svg

# Env
.env
.env.*
!.env.example

# Editor/OS
.vscode/
.idea/
.DS_Store

# Docker volumes (if bind-mounted locally)
/pgdata/
